name: Monorepo pipeline - build and deploy

on:
  push:
    branches:
      - chore/runner-debug

defaults:
  run:
    shell: bash

jobs:
  prepare:
    runs-on: arc-runners
    timeout-minutes: 90

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: debug
        run: |
          echo "hallo"
