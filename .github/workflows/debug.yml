name: Debug

on:
  push:
    branches:
      - fix/back-to-arc-docker

defaults:
  run:
    shell: bash

jobs:
  prepare:
    runs-on: arc-docker
    timeout-minutes: 90

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: debug
        run: |
          echo "hallo"


