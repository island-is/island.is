```markdown
# Island UI Contentful

The `island-ui-contentful` library is a component of a suite of tools generated with [Nx](https://nx.dev). It is designed to support island UI components integrated with Contentful, a versatile content management system.

## Running Unit Tests

Ensure code reliability and correctness by running unit tests. To execute the tests for the `island-ui-contentful` library, use the following command:

```bash
ng test island-ui-contentful
```

The tests are executed using [Jest](https://jestjs.io), a comprehensive and widely used testing framework.
```