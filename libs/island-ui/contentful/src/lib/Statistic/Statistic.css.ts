import { style } from '@vanilla-extract/css'

export const container = style({
  display: 'flex',
  minWidth: '204px',
  height: '204px',
  borderRadius: '50%',
  background: 'white',
})

export const value = style({
  fontSize: '45px',
})
