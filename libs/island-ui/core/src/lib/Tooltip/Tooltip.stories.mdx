import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks'

import { Tooltip } from './Tooltip'
import { Inline } from '../Inline/Inline'
import { Box } from '../Box/Box'
import { Text } from '../Text/Text'

export const tooltipText =
  'Check out this cool tooltip. It has some text in it that you can read for more details.'

<Meta title="Components/Tooltip" component={Tooltip} />

# Tooltip

<br />

### With default icon

<Canvas>
  <Story name="Default">
    <Box padding="gutter">
      <Text variant="p" as="span">
        Here is some text that has a tooltip with the default icon at the end of
        it.{' '}
        <Tooltip
          colored={false}
          placement="right"
          as="button"
          text={tooltipText}
        />
      </Text>
    </Box>
  </Story>
</Canvas>

### With children

<Canvas>
  <Story name="WithChildren">
    <Box>
      <Box padding="gutter">
        <Text>
          Here is some text that has a tooltip wrapped around{' '}
          <Tooltip colored={false} placement="top" as="span" text="text">
            <span>this text</span>
          </Tooltip>
          .
        </Text>
      </Box>
      <Box padding="gutter">
        <Inline space={1}>
          <Text>Here is some text.</Text>
          <Tooltip colored={false} placement="bottom" text="text" />
        </Inline>
      </Box>
    </Box>
  </Story>
</Canvas>

# Props

<Props of={Tooltip} />
