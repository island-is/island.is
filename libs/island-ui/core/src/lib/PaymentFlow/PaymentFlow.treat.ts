import { theme } from '@island.is/island-ui/theme'
import { style } from 'treat'

export const root = style({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
})

export const frame = style({
  width: 660,
  // background: theme.color.blueberry200,
})
