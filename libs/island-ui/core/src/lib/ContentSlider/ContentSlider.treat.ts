import { style } from 'treat'

export const container = style({
  width: '100%',
  overflowX: 'auto',
  paddingBottom: '100px',
  marginBottom: '-100px',
  scrollbarWidth: 'none',
})

export const slideContainer = style({
  display: 'inline-flex',
})
