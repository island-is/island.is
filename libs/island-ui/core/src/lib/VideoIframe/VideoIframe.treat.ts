import { style } from 'treat'

export const container = style({
  position: 'relative',
  paddingBottom: '56.25%',
  height: 0,
})

export const iframe = style({
  position: 'absolute',
  top: 0,
  left: 0,
  width: '100%',
  height: '100%',
})
