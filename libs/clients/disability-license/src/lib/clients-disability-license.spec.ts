import { clientsDisabilityLicense } from './clients-disability-license'

describe('clientsDisabilityLicense', () => {
  it('should work', () => {
    expect(clientsDisabilityLicense()).toEqual('clients-disability-license')
  })
})
