import { clientsElectronicRegistrations } from './clients-electronic-registrations'

describe('clientsElectronicRegistrations', () => {
  it('should work', () => {
    expect(clientsElectronicRegistrations()).toEqual(
      'clients-electronic-registrations',
    )
  })
})
