import { clientsSmartsolutions } from './clients-smartsolutions'

describe('clientsSmartsolutions', () => {
  it('should work', () => {
    expect(clientsSmartsolutions()).toEqual('clients-smartsolutions')
  })
})
