import { clientsFishingLicence } from './clients-fishing-licence'

describe('clientsFishingLicence', () => {
  it('should work', () => {
    expect(clientsFishingLicence()).toEqual('clients-fishing-licence')
  })
})
