import { clientsFirearmLicense } from './clients-firearm-license'

describe('clientsFirearmLicense', () => {
  it('should work', () => {
    expect(clientsFirearmLicense()).toEqual('clients-firearm-license')
  })
})
