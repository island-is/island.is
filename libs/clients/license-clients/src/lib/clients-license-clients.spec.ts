import { clientsLicenseClients } from './clients-license-clients'

describe('clientsLicenseClients', () => {
  it('should work', () => {
    expect(clientsLicenseClients()).toEqual('clients-license-clients')
  })
})
