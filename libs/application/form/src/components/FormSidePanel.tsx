import React, { FC } from 'react'

// TODO this definitely needs more ui improvements
const SidePanel: FC<{ children: React.ReactNode }> = ({ children }) => {
  return <div>{children}</div>
}
export default SidePanel
