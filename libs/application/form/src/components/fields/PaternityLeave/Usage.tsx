import React, { FC } from 'react'
import { FieldBaseProps } from '../../../types'

const PaternityLeaveUsage: FC<FieldBaseProps> = ({
  error,
  field,
  formValue,
}) => {
  return <p>Usage component comes here</p>
}

export default PaternityLeaveUsage
