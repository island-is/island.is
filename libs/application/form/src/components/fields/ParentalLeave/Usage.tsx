import React, { FC } from 'react'
import { FieldBaseProps } from '../../../types'

const ParentalLeaveUsage: FC<FieldBaseProps> = ({
  error,
  field,
  formValue,
}) => {
  return <p>Usage component comes here</p>
}

export default ParentalLeaveUsage
