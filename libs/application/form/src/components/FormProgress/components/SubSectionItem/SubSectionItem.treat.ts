import { style } from 'treat'

export const icon = style({
  position: 'relative',
  display: 'inline-block',
  top: '-4px',
  left: 'calc(50% - 4px)',
})

export const bullet = style({
  display: 'inline-block',
  width: '32px',
})
