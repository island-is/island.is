import { ParentalLeave } from '@island.is/vmst-client'
import { Application } from '@island.is/application/core'

export const transformApplicationToParentalLeaveDTO = (
  application: Application, // eslint-disable-line @typescript-eslint/no-unused-vars
): ParentalLeave => {
  throw new Error(
    'TODO transform application object to ParentalLeave DTO for API',
  )
}
