import { ChargeItemCode } from '@island.is/shared/constants'

export const getChargeItemCodes = (): Array<string> => {
  //TODO WHEN CHARGE ITEM CODE IS AVAILABLE
  return [ChargeItemCode.UNIVERSITY_APPLICATION.toString()]
}
