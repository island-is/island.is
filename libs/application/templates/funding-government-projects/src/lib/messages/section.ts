import { defineMessages } from 'react-intl'

export const section = {
  stepOne: defineMessages({
    pageTitle: {
      id: 'affgp.application:stepOne',
      defaultMessage: 'Step one',
      description: 'Step one of application',
    },
  }),
}
