export type RegisteredChildren = {
    id: string,
    name: string,
    address: string,
    postalCode: string,
    city: string
}

export type Parent = {
    id: string,
    name: string,
    address: string,
    postalCode: string,
    city: string,
    ssn: string
}
