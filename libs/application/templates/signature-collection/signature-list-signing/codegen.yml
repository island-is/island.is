schema:
  - apps/api/src/api.graphql
generates:
  libs/application/templates/signature-collection/signature-list-signing/codegen.yml/src/types/schema.ts:
    plugins:
      - typescript
      - typescript-operations
hooks:
  afterAllFileWrite:
    - prettier --write
