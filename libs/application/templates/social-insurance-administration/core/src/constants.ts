export enum RatioType {
  YEARLY = 'yearly',
  MONTHLY = 'monthly',
}

export enum Actions {
  SEND_APPLICATION = 'sendApplication',
}

export enum BankAccountType {
  ICELANDIC = 'icelandic',
  FOREIGN = 'foreign',
}
