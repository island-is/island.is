import React, { FC } from 'react'
import { FieldBaseProps } from '@island.is/application/core'

export const PendingRejectedTryAgain: FC<FieldBaseProps> = ({
  application,
}) => {
  return <h2>Pending rejected, try again</h2>
}
