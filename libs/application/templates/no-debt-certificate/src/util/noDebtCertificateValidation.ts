import { ApplicationContext } from '@island.is/application/core'

export function hasDebt(context: ApplicationContext): boolean {
  // const { validation } =
  //   (context.application.externalData.noDebtCertificate.data as {
  //     validation: { hasDebt: boolean }
  //   }) || {}
  // const certificate = validation?.certificate
  // return certificate
}
