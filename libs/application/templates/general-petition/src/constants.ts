export enum ApiModuleActions {
  CreateEndorsementList = 'createEndorsementList',
}

export enum States {
  DRAFT = 'draft',
  DONE = 'done',
}

export enum Roles {
  APPLICANT = 'applicant',
  SIGNATUREE = 'signaturee',
}
