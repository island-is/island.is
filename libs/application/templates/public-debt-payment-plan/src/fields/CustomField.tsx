import React from 'react'
import { FieldBaseProps } from '@island.is/application/core'

export const CustomField = (props: FieldBaseProps) => {
  console.log('field base props', props)
  return <div>CustomField</div>
}
