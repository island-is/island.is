import { theme } from '@island.is/island-ui/theme'
import { style } from '@vanilla-extract/css'

export const fastTrackLink = style({
  display: 'inline',
  color: theme.color.blue400,
  textDecoration: 'underline',
})
