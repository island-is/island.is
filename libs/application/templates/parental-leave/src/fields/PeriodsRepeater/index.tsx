import React, { FC } from 'react'
import { RepeaterProps } from '@island.is/application/core'

const PeriodsRepeater: FC<RepeaterProps> = ({
  error,
  repeater,
  application,
}) => {
  return <p>Here we will get the repeater.</p>
}

export default PeriodsRepeater
