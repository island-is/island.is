import React, { FC } from 'react'
import { FieldBaseProps } from '@island.is/application/core'

const ParentalLeavePeriods: FC<FieldBaseProps> = ({
  error,
  field,
  formValue,
}) => {
  return <p>Periods component comes here</p>
}

export default ParentalLeavePeriods
