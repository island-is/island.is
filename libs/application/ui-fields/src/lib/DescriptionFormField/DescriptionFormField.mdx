import {
  Meta,
  Story,
  Canvas,
  Controls,
  Description,
  Source,
} from '@storybook/blocks'
import { dedent } from 'ts-dedent'
import * as DescriptionFormFieldStories from './DescriptionFormField.stories'

import { DescriptionFormField } from './DescriptionFormField'

export const createMockApplication = (data = {}) => ({
  id: '123',
  assignees: [],
  state: data.state || 'draft',
  applicant: '111111-3000',
  typeId: data.typeId || 'ExampleForm',
  modified: new Date(),
  created: new Date(),
  attachments: {},
  answers: data.answers || {},
  externalData: data.externalData || {},
})

<Meta of={DescriptionFormFieldStories} />

# DescriptionFormField

### Usage in a template

You can create a DescriptionFormField using the following function `buildDescriptionField`.

<Source
  language="typescript"
  code={dedent(`
    buildDescriptionField({
      id: 'field.id',
      description: 'Field description',
    })`)}
/>

The previous configuration object will result in the following component:

<Canvas>
  <Story of={DescriptionFormFieldStories.Default} />
</Canvas>

You can also use this field into a custom component by using `<DescriptionFormField field={...} />` with the configuration object defined above.

# Props

## DescriptionFormField

<Controls />
