import {
  Meta,
  Story,
  Canvas,
  Controls,
  Description,
  Source,
} from '@storybook/blocks'
import { dedent } from 'ts-dedent'
import * as RadioFormFieldStories from './RadioFormField.stories'

import { RadioFormField } from './RadioFormField'

export const createMockApplication = (data = {}) => ({
  id: '123',
  assignees: [],
  state: data.state || 'draft',
  applicant: '111111-3000',
  typeId: data.typeId || 'ExampleForm',
  modified: new Date(),
  created: new Date(),
  attachments: {},
  answers: data.answers || {},
  externalData: data.externalData || {},
})

<Meta of={RadioFormFieldStories} />

# RadioFormField

### Usage in a template

You can create a RadioFormField using the following function `buildRadioField`.

<Source
  language="typescript"
  code={dedent(`
    buildRadioField({
      id: 'field.id',
      title: 'Field title',
      options: [
        {
          value: 'yes',
          label: 'Yes',
        },
        {
          value: 'no',
          label: 'No',
        },
      ],
      width: 'half',
    })`)}
/>

The previous configuration object will result in the following component:

<Canvas>
  <Story of={RadioFormFieldStories.Default} />
</Canvas>

You can also use this field into a custom component by using `<RadioFormField field={...} />` with the configuration object defined above.

# Props

## RadioFormField

<Controls />
