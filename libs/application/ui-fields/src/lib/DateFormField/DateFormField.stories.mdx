import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
  Source,
} from '@storybook/addon-docs'
import { dedent } from 'ts-dedent'

import { DateFormField } from './DateFormField'

export const createMockApplication = (data = {}) => ({
  id: '123',
  assignees: [],
  state: data.state || 'draft',
  applicant: '111111-3000',
  typeId: data.typeId || 'ExampleForm',
  modified: new Date(),
  created: new Date(),
  attachments: {},
  answers: data.answers || {},
  externalData: data.externalData || {},
})

<Meta title="Application System/DateFormField" component={DateFormField} />

# DateFormField

### Usage in a template

You can create a DateFormField using the following function `buildDateField`.

<Source
  language="typescript"
  code={dedent(`
    buildRadioField({
      id: 'field.id',
      title: 'Field title',
      placeholder: 'Select a date',
    })`)}
/>

The previous configuration object will result in the following component:

<Canvas>
  <Story name="Default" height="425px">
    <DateFormField
      application={createMockApplication()}
      field={{
        id: 'field.id',
        title: 'Field title',
        placeholder: 'Select a date',
        backgroundColor: 'blue',
      }}
    />
  </Story>
</Canvas>

You can also use this field into a custom component by using `<DateFormField field={...} />` with the configuration object defined above.

# Props

## DateFormField

<ArgsTable of={DateFormField} />
