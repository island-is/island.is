import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
  Source,
} from '@storybook/addon-docs'
import { dedent } from 'ts-dedent'

import { PhoneFormField } from './PhoneFormField'

export const createMockApplication = (data = {}) => ({
  id: '123',
  assignees: [],
  state: data.state || 'draft',
  applicant: '111111-3000',
  typeId: data.typeId || 'ExampleForm',
  modified: new Date(),
  created: new Date(),
  attachments: {},
  answers: data.answers || {},
  externalData: data.externalData || {},
})

<Meta title="Application System/PhoneFormField" component={PhoneFormField} />

# PhoneFormField

### Usage in a template

You can create a PhoneFormField using the following function `buildPhoneField`.

<Source
  language="typescript"
  code={dedent(`
    buildPhoneField({
      id: 'field.id',
      title: 'Field title',
      placeholder: 'Enter your email',
    })`)}
/>

The previous configuration object will result in the following component:

<Canvas>
  <Story name="Default">
    <PhoneFormField
      application={createMockApplication()}
      field={{
        id: 'field.id',
        title: 'Field title',
        placeholder: 'Enter your phone',
        backgroundColor: 'blue',
      }}
      showFieldName
    />
  </Story>
</Canvas>

You can also use this field into a custom component by using `<PhoneFormField field={...} />` with the configuration object defined above.

# Props

## PhoneFormField

<ArgsTable of={PhoneFormField} />
