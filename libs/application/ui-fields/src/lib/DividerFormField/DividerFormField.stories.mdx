import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
  Source,
} from '@storybook/addon-docs'
import { dedent } from 'ts-dedent'

import { DividerFormField } from './DividerFormField'

export const createMockApplication = (data = {}) => ({
  id: '123',
  assignees: [],
  state: data.state || 'draft',
  applicant: '111111-3000',
  typeId: data.typeId || 'ExampleForm',
  modified: new Date(),
  created: new Date(),
  attachments: {},
  answers: data.answers || {},
  externalData: data.externalData || {},
})

<Meta
  title="Application System/DividerFormField"
  component={DividerFormField}
/>

# DividerFormField

### Usage in a template

You can create a DividerFormField using the following function `buildDividerField`.

<Source language="typescript" code={`buildDividerField({})`} />

The previous configuration object will result in the following component:

<Canvas>
  <Story name="Default">
    <DividerFormField application={createMockApplication()} field={{}} />
  </Story>
</Canvas>

You can also use this field into a custom component by using `<DividerFormField field={...} />` with the configuration object defined above.

# Props

## DividerFormField

<ArgsTable of={DividerFormField} />
