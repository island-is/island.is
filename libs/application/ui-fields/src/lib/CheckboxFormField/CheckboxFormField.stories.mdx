import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
  Source,
} from '@storybook/addon-docs'
import { dedent } from 'ts-dedent'

import { CheckboxFormField } from './CheckboxFormField'

export const createMockApplication = (data = {}) => ({
  id: '123',
  assignees: [],
  state: data.state || 'draft',
  applicant: '111111-3000',
  typeId: data.typeId || 'ExampleForm',
  modified: new Date(),
  created: new Date(),
  attachments: {},
  answers: data.answers || {},
  externalData: data.externalData || {},
})

<Meta
  title="Application System/CheckboxFormField"
  component={CheckboxFormField}
/>

# CheckboxFormField

### Usage in a template

You can create a CheckboxFormField using the following function `buildCheckboxField`.

<Source
  language="typescript"
  code={dedent(`
    buildCheckboxField({
      id: 'field.id',
      title: 'Field title',
      width: 'half',
      options: [
        {
          value: 'phone',
          label: 'Phone',
          subLabel: 'Used to send notifications.',
        },
        {
          value: 'email',
          label: 'Email',
          subLabel: 'Used to send newsletters.',
        },
      ],
    })`)}
/>

The previous configuration object will result in the following component: This is the default UI recommended for the Checkbox field.

<Canvas>
  <Story name="Default">
    <CheckboxFormField
      application={createMockApplication()}
      field={{
        id: 'field.id',
        title: 'Field title',
        large: true,
        backgroundColor: 'blue',
        width: 'half',
        options: [
          {
            value: 'phone',
            label: 'Phone',
            subLabel: 'Used to send notifications.',
          },
          {
            value: 'email',
            label: 'Email',
            subLabel: 'Used to send newsletters.',
          },
        ],
      }}
    />
  </Story>
</Canvas>

A more simple of choices should be displayed as follow:

<Canvas>
  <Story name="List of checkboxes">
    <CheckboxFormField
      application={createMockApplication()}
      field={{
        id: 'field.id',
        title: 'Field title',
        options: [
          {
            value: 'apple',
            label: 'Apple',
          },
          {
            value: 'facebook',
            label: 'Facebook',
          },
          {
            value: 'google',
            label: 'Google',
          },
        ],
      }}
    />
  </Story>
</Canvas>

You can also use this field into a custom component by using `<CheckboxFormField field={...} />` with the configuration object defined above.

# Props

## CheckboxFormField

<ArgsTable of={CheckboxFormField} />
