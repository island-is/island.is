import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
  Source,
} from '@storybook/addon-docs'
import { dedent } from 'ts-dedent'

import { TextFormField } from './TextFormField'

export const createMockApplication = (data = {}) => ({
  id: '123',
  assignees: [],
  state: data.state || 'draft',
  applicant: '111111-3000',
  typeId: data.typeId || 'ExampleForm',
  modified: new Date(),
  created: new Date(),
  attachments: {},
  answers: data.answers || {},
  externalData: data.externalData || {},
})

<Meta title="Application System/TextFormField" component={TextFormField} />

# TextFormField

### Usage in a template

You can create a TextFormField using the following function `buildTextField`.

<Source
  language="typescript"
  code={dedent(`
    buildTextField({
      id: 'field.id',
      title: 'Field title',
      placeholder: 'Enter your email',
    })`)}
/>

The previous configuration object will result in the following component:

<Canvas>
  <Story name="Default">
    <TextFormField
      application={createMockApplication()}
      field={{
        id: 'field.id',
        title: 'Field title',
        placeholder: 'Enter your email',
        backgroundColor: 'blue',
      }}
      showFieldName
    />
  </Story>
</Canvas>

You can also use this field into a custom component by using `<TextFormField field={...} />` with the configuration object defined above.

# Props

## TextFormField

<ArgsTable of={TextFormField} />
