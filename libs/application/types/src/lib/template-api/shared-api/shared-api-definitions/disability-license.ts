import { defineTemplateApi } from '../../TemplateApi'

export const DisabiltyLicenseApi = defineTemplateApi({
  action: 'hasDisability',
  namespace: 'DisabilityLicenseShared',
  externalDataId: 'hasDisability',
})
