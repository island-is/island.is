import { libsPortalsFormSystemUi } from './libs/portals/form-system/ui'

describe('libsPortalsFormSystemUi', () => {
  it('should work', () => {
    expect(libsPortalsFormSystemUi()).toEqual('libs/portals/form-system/ui')
  })
})
