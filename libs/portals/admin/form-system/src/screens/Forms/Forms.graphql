query formSystemGetForms($input: FormSystemGetFormsInput!) {
  formSystemGetForms(input: $input) {
    forms {
      id
      name {
        is
        en
      }
      created
      lastChanged
      organization {
        id
      }
      isTranslated
    }
  }
}
