query GetPaperMailList($input: DocumentProviderPaperMailInput!) {
  getPaperMailList(input: $input) {
    paperMail {
      nationalId
      origin
      wantsPaper
      dateAdded
      dateUpdated
    }
    totalCount
  }
}
