mutation BulkCompare($input: SignatureCollectionNationalIdsInput!) {
  signatureCollectionBulkCompareSignaturesAllLists(input: $input) {
    id
    listId
    listTitle
    signee {
      name
      nationalId
      address
    }
    isDigital
    active
    created
  }
}
