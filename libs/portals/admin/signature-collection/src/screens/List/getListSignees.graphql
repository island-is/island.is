query Signatures($input: SignatureCollectionIdInput!) {
  signatureCollectionAdminSignatures(input: $input) {
    id
    listId
    signee {
      name
      nationalId
      address
    }
    isDigital
    pageNumber
    active
    created
  }
}
