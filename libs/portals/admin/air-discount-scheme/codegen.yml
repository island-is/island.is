schema:
  - apps/api/src/api.graphql
documents:
  - libs/portals/admin/air-discount-scheme/src/**/*.graphql
generates:
  libs/portals/admin/air-discount-scheme/src/:
    preset: 'near-operation-file'
    presetConfig:
      baseTypesPath: '~@island.is/api/schema'
    plugins:
      - typescript-operations
      - typescript-react-apollo
    config:
      scalars:
        DateTime: Date
hooks:
  afterAllFileWrite:
    - prettier --write
