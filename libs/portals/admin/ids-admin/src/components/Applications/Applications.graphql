# Get Applications by TenantId
query GetApplicationsById($input: ApplicationsInput!) {
  authAdminApplications(input: $input) {
    data {
      applicationId
      applicationType
      availableEnvironments
      defaultEnvironment {
        id
        environment
        name
        displayName {
          locale
          value
        }
      }
    }
  }
}
