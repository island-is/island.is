mutation CreateApplication($input: CreateAuthAdminApplicationInput!) {
  createAuthAdminApplication(input: $input) {
    applicationId
    applicationType
    environments {
      displayName {
        value
      }
      environment
      id
      name
    }
  }
}
