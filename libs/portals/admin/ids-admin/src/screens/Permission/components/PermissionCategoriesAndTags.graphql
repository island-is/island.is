query GetScopeCategories($lang: String) {
  authAdminScopeCategories(lang: $lang) {
    id
    title
    slug
    description
  }
}

query GetScopeTags($lang: String) {
  authAdminScopeTags(lang: $lang) {
    id
    title
    slug
    intro
  }
}
