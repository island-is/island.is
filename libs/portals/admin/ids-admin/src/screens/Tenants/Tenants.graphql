query Tenants {
  authAdminTenants {
    data {
      id
      availableEnvironments
      defaultEnvironment {
        id
        name
        environment
        displayName {
          locale
          value
        }
      }
    }
  }
}
