import { style } from '@vanilla-extract/css'

/**
 * Shared max width for every key-info edit screen (language, health,
 * emergency contact) so the forms line up consistently.
 */
export const formContainer = style({
  maxWidth: 432,
})
