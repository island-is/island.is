import { style } from '@vanilla-extract/css'

export const form = style({
  width: '100%',
})

export const subtitleContainer = style({
  minHeight: 30,
})

export const noCodeReceivedLoadingWrapper = style({
  display: 'flex',
  alignItems: 'center',
})
