query GetTreatmentQuestionnaires($treatmentId: ID!, $locale: String!) {
  questionnairesTreatmentList(treatmentId: $treatmentId, locale: $locale) {
    questionnaires {
      ...QuestionnaireListItem
    }
  }
}
