query GetHealthTreatmentDocuments($treatmentId: ID!) {
  healthDirectorateTreatmentDocuments(treatmentId: $treatmentId) {
    id
    title
    groupName
    sentAt
    links {
      label
      href
    }
  }
}
