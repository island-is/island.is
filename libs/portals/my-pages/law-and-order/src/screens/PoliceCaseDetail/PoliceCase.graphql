query GetPoliceCase($input: LawAndOrderPoliceCaseInput!) {
  lawAndOrderPoliceCase(input: $input) {
    number
    type
    status
    contact
    courtAdvocate
    department
    received
    receivedLocation
    modified
  }
}
