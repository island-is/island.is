query GetCourtCases($locale: String!) {
  lawAndOrderCourtCasesList(locale: $locale) {
    __typename
    cases {
      __typename
      cacheId
      id
      caseNumberTitle
      type
      state {
        label
        color
      }
    }
  }
}
