query ShipRegistrySailorRightCertificates {
  shipRegistrySailor {
    certificates {
      rightCertificates {
        id
        type
        rightsCategories
        validToDate
        status
        certificateNumber
        issueDate
      }
    }
  }
}
