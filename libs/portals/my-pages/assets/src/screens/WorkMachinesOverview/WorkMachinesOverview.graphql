query GetWorkMachines($input: WorkMachinesCollectionInput) {
  workMachinesPaginatedCollection(input: $input) {
    data {
      id
      registrationNumber
      typeBreakdown {
        fullTypeName
      }
      status
      dateLastInspection
    }
    linkCollection {
      href
      relation
      relationCategory
      displayTitle
    }
    totalCount
  }
}
