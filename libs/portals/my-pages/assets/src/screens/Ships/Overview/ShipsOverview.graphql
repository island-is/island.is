query ShipsOverview {
  shipRegistryUserShips {
    data {
      id
      name
      regionAcronym
      seaworthiness {
        isValid
        validTo
      }
    }
    totalCount
    pageInfo {
      hasNextPage
    }
  }
}
