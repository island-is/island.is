# my-pages

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build my-pages` to build the library.

## Running unit tests

Run `nx test my-pages` to execute the unit tests via [Jest](https://jestjs.io).
