import { apiDomainsLawAndOrder } from './api-domains-law-and-order'

describe('apiDomainsLawAndOrder', () => {
  it('should work', () => {
    expect(apiDomainsLawAndOrder()).toEqual('api-domains-law-and-order')
  })
})
