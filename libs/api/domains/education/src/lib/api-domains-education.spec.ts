import { apiDomainsEducation } from './api-domains-education'

describe('apiDomainsEducation', () => {
  it('should work', () => {
    expect(apiDomainsEducation()).toEqual('api-domains-education')
  })
})
