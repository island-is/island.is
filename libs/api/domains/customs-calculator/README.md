# api-domains-customs-calculator

GraphQL API domain for the Skatturinn customs calculator endpoints.

## Running unit tests

Run `nx test api-domains-customs-calculator` to execute the unit tests via [Jest](https://jestjs.io).
