import { apiDomainsFishingLicence } from './api-domains-fishing-licence'

describe('apiDomainsFishingLicence', () => {
  it('should work', () => {
    expect(apiDomainsFishingLicence()).toEqual('api-domains-fishing-licence')
  })
})
