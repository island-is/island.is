import { apiDomainsHackathonPhi } from './api-domains-hackathon-phi'

describe('apiDomainsHackathonPhi', () => {
  it('should work', () => {
    expect(apiDomainsHackathonPhi()).toEqual('api-domains-hackathon-phi')
  })
})
