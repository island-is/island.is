import { apiDomainsFishingLicense } from './api-domains-fishing-license'

describe('apiDomainsFishingLicence', () => {
  it('should work', () => {
    expect(apiDomainsFishingLicense()).toEqual('api-domains-fishing-licene')
  })
})
