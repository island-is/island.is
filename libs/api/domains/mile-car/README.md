# api-domains-mile-car

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test api-domains-mile-car` to execute the unit tests via [Jest](https://jestjs.io).

## Running lint

Run `nx lint api-domains-mile-car` to execute the lint via [ESLint](https://eslint.org/).
