schema:
  - apps/api/src/api.graphql
generates:
  ./libs/api/mocks/src/graphql/types.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      noSchemaStitching: true
      contextType: ./handler#Context
hooks:
  afterAllFileWrite:
    - prettier --write
