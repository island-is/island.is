{{#> center}}
  {{#if eyebrow}}
    <h2 class="heading heading-eyebrow text-{{ternary align align 'center'}}">
      {{{eyebrow}}}
    </h2>
  {{/if}}
  <a class="heading text-{{ternary align align 'center'}} size-{{ternary small 'small' 'normal'}}">
    {{{copy}}}
  </a>
{{/center}}
