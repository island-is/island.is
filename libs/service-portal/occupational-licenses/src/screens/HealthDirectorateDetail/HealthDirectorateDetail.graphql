query getHealthDirectorateLicenseById($id: String!) {
  getOrganizations {
    items {
      id
      title
      slug
      logo {
        id
        url
        title
      }
    }
  }
  OccupationalLicensesHealthDirectorateLicense(id: $id) {
    holderName
    profession
    type
    number
    validFrom
    isValid
  }
}
