query getEducationalLicenseById($id: String!) {
  getOrganizations {
    items {
      id
      title
      slug
      logo {
        id
        url
        title
      }
    }
  }
  OccupationalLicensesEducationalLicense(id: $id) {
    id
    type
    profession
    validFrom
    downloadUrl
    isValid
  }
}
