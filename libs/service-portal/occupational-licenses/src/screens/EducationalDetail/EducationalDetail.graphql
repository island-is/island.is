query getEducationalLicenseById($id: String!) {
  getOrganizations {
    items {
      id
      title
      slug
      logo {
        id
        url
        title
      }
    }
  }
  OccupationalLicensesEducationalLicense(id: $id) {
    id
    school
    programme
    date
    url
    isValid
  }
}
