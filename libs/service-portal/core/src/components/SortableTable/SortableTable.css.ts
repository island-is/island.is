import { style } from '@vanilla-extract/css'

export const chevron = style({
  position: 'absolute',
  right: -20,
  top: 0,
  bottom: 0,
  margin: 'auto',
})

export const btn = style({
  position: 'relative',
})
