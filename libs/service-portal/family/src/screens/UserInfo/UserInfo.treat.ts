import { style } from 'treat'

export const avatar = style({
  width: 80,
  height: 80,
})

export const uploadPictureContainer = style({
  border: '1px dashed #99C0FF', //TODO FIX COLOR
  borderRadius: '5px',
  width: '100%',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
})
