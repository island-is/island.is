import { style } from 'treat'
import { theme } from '@island.is/island-ui/theme'

export const WipBoxSmall = style({
  background: theme.color.blue100,
  borderRadius: theme.border.radius.standard,
  height: '16px',
})
export const WipBoxLarge = style({
  background: theme.color.blue100,
  borderRadius: theme.border.radius.standard,
  height: '24px',
})
