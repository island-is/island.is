query getDentists($input: DentistBillsInput) {
  rightsPortalUserDentist(input: $input) {
    currentDentistName
    billHistory {
      number
      amount
      coveredAmount
      date
      refundDate
    }
  }
}
