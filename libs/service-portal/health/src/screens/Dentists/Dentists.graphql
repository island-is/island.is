query getDentists($input: DentistBillsInput) {
  rightsPortalUserDentistRegistration(input: $input) {
    currentDentistName
    billHistory {
      number
      amount
      coveredAmount
      date
      refundDate
    }
  }
}
