query getHealthCenter($input: HealthCenterHistoryInput) {
  rightsPortalHealthCenterHistory(input: $input) {
    current {
      name
      doctor
    }
    history {
      dateFrom
      dateTo
      healthCenter {
        name
        doctor
      }
    }
  }
}
