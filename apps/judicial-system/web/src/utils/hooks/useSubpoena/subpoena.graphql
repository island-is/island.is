query Subpoena($input: SubpoenaQueryInput!) {
  subpoena(input: $input) {
    id
    created
    serviceStatus
    serviceDate
    servedBy
    comment
    defenderNationalId
    caseId
    defendantId
    policeSubpoenaId
  }
}
