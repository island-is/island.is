mutation UpdatePoliceDigitalCaseFiles(
  $input: UpdatePoliceDigitalCaseFilesInput!
) {
  updatePoliceDigitalCaseFiles(input: $input) {
    policeDigitalCaseFiles {
      id
      orderWithinChapter
    }
  }
}
