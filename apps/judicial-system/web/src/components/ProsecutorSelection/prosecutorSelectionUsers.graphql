query ProsecutorSelectionUsers {
  users(input: { role: PROSECUTOR }) {
    id
    created
    modified
    name
    nationalId
    mobileNumber
    role
    email
    title
    institution {
      id
      type
      name
    }
    active
  }
}
