import { style } from 'treat'

export const policeCaseNumberSection = style({
  marginBottom: 56,
})

export const titleContainer = style({
  marginBottom: 16,
})

export const inputContainer = style({
  marginBottom: 16,
})
