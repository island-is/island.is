import { style } from 'treat'
import { theme } from '@island.is/island-ui/theme'

export const custodyEndDateContainer = style({
  display: 'grid',
  gridTemplateColumns: '1fr 264px',
  columnGap: theme.spacing[1],
})
