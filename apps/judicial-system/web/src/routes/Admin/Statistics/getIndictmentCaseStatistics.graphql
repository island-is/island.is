query IndictmentCaseStatistics($input: IndictmentStatisticsInput) {
  indictmentCaseStatistics(input: $input) {
    count
    inProgressCount
    rulingCount
    averageRulingTimeMs
    averageRulingTimeDays
  }
}
