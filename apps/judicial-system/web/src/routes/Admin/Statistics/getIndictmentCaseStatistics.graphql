query IndictmentCaseStatistics($input: CaseStatisticsInput) {
  indictmentCaseStatistics(input: $input) {
    count
    inProgressCount
    rulingCount
    averageRulingTimeMs
    averageRulingTimeDays
  }
}
