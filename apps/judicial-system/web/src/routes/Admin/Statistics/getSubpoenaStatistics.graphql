query SubpoenaStatistics($input: CaseStatisticsInput) {
  subpoenaStatistics(input: $input) {
    count
    serviceStatusStatistics {
      serviceStatus
      count
      averageServiceTimeMs
      averageServiceTimeDays
    }
  }
}
