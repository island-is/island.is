query CourtOfAppealsCasesInProgress($input: CaseTableQueryInput!) {
  caseTable(input: $input) {
    type
    columnCount
    rowCount
    columns {
      title
      type
    }
    rows {
      caseId
      cells {
        value
      }
    }
  }
}
