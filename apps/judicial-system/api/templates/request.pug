doctype html
html
  head
  body
    h1 Krafa um gæsluvarðhald
    p LÖKE málsnúmer #{policeCaseNumber}
    p Dómstóll: #{court}

    h2 Grunnupplýsingar
    p Kennitala: #{accusedNationalId}
    p Fullt nafn: #{accusedName}
    p Lögheimili: #{accusedAddress}

    h3 Tími handtöku
    p #{arrestDate} kl. #{arrestTime}.

    h3 Ósk um fyrirtökudag og tíma
    p #{requestedCourtDate} kl. #{requestedCourtTime}.

    h3 Dómkröfur
    p Gæsluvarðhald til #{requestedCustodyEndDate} kl. #{requestedCustodyEndTime}.

    h3 Lagaákvæði
    each par in lawsBroken.split('\n')
      p #{par}
    p

    h3 Takmarkanir á gæslu
    p #{custodyRestrictions}

    h2 Greinargerð um málsatvik og lagarök

    h3 Málsatvik rakin
    each par in caseFacts.split('\n')
      p #{par}

    h3 Framburðir
    each par in witnessAccounts.split('\n')
      p #{par}

    h3 Staða rannsóknar og næstu skref
    each par in investigationProgress.split('\n')
      p #{par}

    h3 Lagarök
    each par in legalArguments.split('\n')
      p #{par}

    p <strong>Fhl. #{prosecutorName}, #{prosecutorTitle}</strong>
