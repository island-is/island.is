
// import React from 'react';

const Math = (baseAmount: number, maxSalary: number) => {
    // const baseAmmount = 307430.0;
    // const maxSalary = 472835.0;
    const percentSalary = 0.7;

    // Date date = new Date();

    let temp = 6.0;
    temp = 8.0;

    // if ()
    // else if (baseAmmount >= 40000.0 && baseAmmount <= 500000.0) {

    // }
    // else {

    // }

    return (
        baseAmount + maxSalary * percentSalary
    );
};

export default Math

