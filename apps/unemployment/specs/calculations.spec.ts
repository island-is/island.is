// import React from 'react'
import { makeCalculations } from '../utils/overview-calculations'

describe('Calculations', () => {
  it('should calculate correctly', () => {
    makeCalculations({})
  })
})
