apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gjafakort-web.fullname" . }}-test-connection"
  labels:
    {{- include "gjafakort-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gjafakort-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
