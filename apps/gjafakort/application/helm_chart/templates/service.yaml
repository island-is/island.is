apiVersion: v1
kind: Service
metadata:
  name: {{ include "gjafakort-application.fullname" . }}
  labels:
    {{- include "gjafakort-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 4242
      protocol: TCP
      name: http
  selector:
    {{- include "gjafakort-application.selectorLabels" . | nindent 4 }}
