import { style } from '@vanilla-extract/css'

export const selectBox = style({
  width: '250px',
})

export const crossmark = style({
  marginLeft: '8px',
  fontSize: '0.75rem',
  fontWeight: 'normal',
})
