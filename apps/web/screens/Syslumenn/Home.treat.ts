import { style } from 'treat'

export const headerBg = style({
  background: '#252973',
  marginBottom: '2px',
  maxHeight: 365,
})

export const headerBorder = style({
  borderBottom: '4px solid #ffbe43',
})

export const headerSidebar = style({
  marginTop: -32,
})

export const intro = style({
  minHeight: 200,
  fontSize: 24,
  lineHeight: 34 / 24,
  fontWeight: 300,
})
