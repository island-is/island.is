import { style } from 'treat'

export const container = style({
  paddingBottom: '56.25%',
  position: 'relative',
  display: 'block',
  width: '100%',
})

export const iframe = style({
  height: '100%',
  width: '100%',
  position: 'absolute',
  top: 0,
  left: 0,
})
