import { style } from '@vanilla-extract/css'

export const readOnlyValues = style({
  padding: '8px 16px 8px 8px',
  boxShadow: 'inset 0 0 0 1px #ccdfff',
  borderRadius: '8px',
})

export const total = style({
  padding: '8px 16px 8px 8px',
  boxShadow: 'inset 0 0 0 1px #ccdfff',
  borderRadius: '8px',
  background: 'white',
})
