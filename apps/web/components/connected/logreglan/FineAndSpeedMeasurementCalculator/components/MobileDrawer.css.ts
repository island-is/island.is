import { style } from '@vanilla-extract/css'

export const container = style({
  position: 'fixed',
  bottom: 0,
  left: 0,
  width: '100%',
  height: '80px',
  display: 'flex',
  alignItems: 'center',
})
