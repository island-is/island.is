import { style } from '@vanilla-extract/css'

export const container = style({
  backgroundColor: '#6A1033',
  paddingTop: '60px',
  paddingBottom: '60px',
})

export const emptyBox = style({
  width: '120px',
})
