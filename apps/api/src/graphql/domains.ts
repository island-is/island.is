import * as helloWorld from '@island.is/api/domains/hello-world'
import * as cms from '@island.is/api/domains/cms'
import * as searcher from '@island.is/api/domains/searcher'

export default [helloWorld, searcher, cms]
