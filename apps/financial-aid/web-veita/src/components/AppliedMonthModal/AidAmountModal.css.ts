import { style } from '@vanilla-extract/css'

export const modal = style({
  display: 'block',
  width: '100%',
  maxWidth: '440px',
  boxShadow: '0px 8px 32px rgba(0, 0, 0, 0.08)',
  borderRadius: '12px',
})
