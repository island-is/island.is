import { style } from '@vanilla-extract/css'

export const upperSeperator = style({
  width: 0,
  flexShrink: 0,
  height: 24,
  border: '1px solid #C3ABD9',
})

export const lowerSeperator = style({
  width: 0,
  flexShrink: 0,
  height: 16,
  border: '1px solid #CCDFFF',
})
