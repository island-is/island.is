schema:
  - apps/api/src/api.graphql
documents: apps/samradsgatt/screens/**/*.{ts,tsx,graphql}
generates:
  apps/samradsgatt/graphql/schema.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      exportFragmentSpreadSubTypes: true
      scalars:
        DateTime: Date
        JSON: '{ [key: string]: any }'
      namingConvention:
        typeNames: change-case#pascalCase
hooks:
  afterAllFileWrite:
    - prettier --write
