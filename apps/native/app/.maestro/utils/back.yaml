appId: ${APP_ID}
---
# For some reason, the back button has different text and id on android and iOS.
- runFlow:
    when:
      platform: 'android'
    commands:
      - tapOn:
          id: 'TOPBAR_BACK_BUTTON'
- runFlow:
    when:
      platform: 'ios'
    commands:
      - tapOn:
          id: 'BackButton'
