query GetUserVehicles($input: GetVehiclesForUserInput!) {
  vehiclesList(input: $input) {
    vehicleList {
      ...VehicleFragment
    }
    paging {
      pageNumber
      pageSize
      totalPages
      totalRecords
    }
  }
}
