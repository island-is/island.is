fragment ApplicationFragment on Application {
  id
  created
  modified
  applicant
  state
  typeId
  name
  progress
  status
  institution
  formSystemSlug
  actionCard {
    draftTotalSteps
    draftFinishedSteps
    tag {
      label
      variant
    }
    pendingAction {
      title
    }
  }
}
