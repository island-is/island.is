fragment ApplicationFragment on Application {
  id
  created
  modified
  applicant
  state
  typeId
  name
  progress
  status
  institution
  actionCard {
    title
    description
    tag {
      label
      variant
    }
    pendingAction {
      displayStatus
      content
      title
    }
    history {
      log
      date
    }
    deleteButton
    draftTotalSteps
    draftFinishedSteps
  }
}
