import HealthMessage from '@/app/(auth)/(tabs)/health/messages/[id]'

// Health message detail rendered inside the notifications modal so opening a
// health-message notification keeps the back arrow pointing at the sheet.
export default HealthMessage
