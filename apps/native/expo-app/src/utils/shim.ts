// require('node-libs-react-native/globals');

// const te = require('@zxing/text-encoding');
// ;(global as any).TextDecoder = te.TextDecoder
// ;(global as any).TextEncoder = te.TextEncoder
// ;(global as any).BigInt = Number;

export const shim = () => {
  // noop
}
