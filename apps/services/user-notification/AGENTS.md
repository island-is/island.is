# Notification logging

Rendered notifications and its `args` may contain sensitive data. Never log the `args` payload, argument values, or rendered notification content. For diagnostics, log only argument keys and value lengths.
