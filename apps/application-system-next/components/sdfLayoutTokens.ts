/**
 * Vertical spacing between field blocks — aligned with common `Box` usage in
 * `libs/application/ui-shell` / `ui-fields` (typically `marginBottom={2}`–`3}`).
 * Tune here for global SDF vs legacy parity.
 */
export const SDF_FIELD_BLOCK_MARGIN_BOTTOM = 2
