export const ACCEPTED_TERMS_AND_CONDITION = 'acceptedTermsAndConditions'

export enum PlateInfo {
  PLATE_LOST = '0',
}

export enum UseStatus {
  OUT_TICKET = '16', // Úr umferð (miði)
}

export enum OutInUsage {
  OUT = 0,
  IN = 1,
}
