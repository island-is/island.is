import { style } from 'treat'
import { theme } from '@island.is/island-ui/theme'

export const serviceList = style({
  display: 'flex',
  flexWrap: 'wrap',
  minHeight: 350,
  flexDirection: 'row',
})
