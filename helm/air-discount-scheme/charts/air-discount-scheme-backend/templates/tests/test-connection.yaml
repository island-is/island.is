apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "air-discount-scheme-backend.fullname" . }}-test-connection"
  {{- if .Values.namespace }}
  namespace: {{ .Values.namespace }}
  {{- end }}
  labels:
    {{- include "air-discount-scheme-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "air-discount-scheme-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
