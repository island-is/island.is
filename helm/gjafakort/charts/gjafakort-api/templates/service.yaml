apiVersion: v1
kind: Service
metadata:
  name: {{ include "gjafakort-api.fullname" . }}
  {{- if .Values.namespace }}
  namespace: {{ .Values.namespace }}
  {{- end }}
  labels:
    {{- include "gjafakort-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3333
      protocol: TCP
      name: http
  selector:
    {{- include "gjafakort-api.selectorLabels" . | nindent 4 }}
