apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "search-indexer-service.fullname" . }}-test-connection"
  {{- if .Values.namespace }}
  namespace: {{ .Values.namespace }}
  {{- end }}
  labels:
    {{- include "search-indexer-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "search-indexer-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
