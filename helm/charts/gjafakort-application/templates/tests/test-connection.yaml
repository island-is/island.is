apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gjafakort-application.fullname" . }}-test-connection"
  {{- if .Values.namespace }}
  namespace: {{ .Values.namespace }}
  {{- end }}
  labels:
    {{- include "gjafakort-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gjafakort-application.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
