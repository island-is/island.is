# vi: ft=gitignore

# See .dockerignore syntox docs for negation (latest line decides)
# https://docs.docker.com/build/concepts/context/#negating-matches

# Known ignores
/.github/
/.git/
# Don't ignore branc/refs for Datadog source code integration
# https://docs.datadoghq.com/integrations/guide/source-code-integration/?tab=nodejs#build-inside-a-docker-container
!.git/HEAD
!.git/config
!.git/refs
/scripts/ci/
/.env.*
/.envrc*
/.nx/

# Cache and packages
**/node_modules/
# Ignoring _all_ cache folders dosen't work, because we have libraries named `cache`
# **/cache/
/.yarn/cache/
# Ignores e.g. `cache/` and `cache_outptut/`
/cache*
/.cache*/

# Logs and temporaries
**/log/
**/*.log
**/tmp/
**/temp/
**/.next/

# Outputs
**/out/

# Docker-stuff
**/Dockerfile
**/Dockerfile.*
**/Containerfile
**/Containerfile.*
**/*-compose.yaml
**/*-compose.yml
